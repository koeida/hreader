# Update

- Added default browser bootstrap wiring: `make install` now installs Python deps and Playwright Chromium via `install-browser`.
- Added CI workflow `.github/workflows/ci.yml` to run `make venv`, `make install`, and `make test` on push/pull_request.
- Updated README setup docs to remove separate manual Chromium step and reflect new default bootstrap.
- Validation run results:
  - `make install`: passed
  - `make test`: passed (`15 passed`)
  - `BASE_URL=http://127.0.0.1:8017 make smoke` with live uvicorn: passed

## Notes
- Default `make smoke` still expects a running API instance; validation used an isolated local port with `BASE_URL` override.
