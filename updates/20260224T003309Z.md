# Update 2026-02-24T00:33:09Z

## What I implemented
- Added browser-executed UI journey test at `tests/test_ui_browser.py`.
  - Runs against a live uvicorn app instance.
  - Exercises DOM-driven flow: create user, create/open text, navigate sentences, jump boundary handling, words pagination, and token-state update propagation.
  - Gracefully skips if Playwright browser runtime is unavailable.
- Fixed SQLite thread-affinity issue in `app/db.py` by opening connections with `check_same_thread=False`, which was exposed by live-server browser testing.
- Upgraded `scripts/smoke.sh` into a stricter fail-fast journey.
  - Validates health, user/text creation, text rename, sentence boundary error handling, paginated words checks, token-state mutation, and progress updates.
- Updated `README.md` with:
  - Browser journey coverage notes.
  - Playwright Chromium install command.
  - Family-ready local run checklist and validation order.
- Updated `requirements.txt` with `playwright` dependency.

## Validation run
- Executed `pytest -q` -> `15 passed`.
- Executed smoke journey with a temporary uvicorn server on an isolated random local port and `scripts/smoke.sh` -> PASS.

## Remaining work
- Wire Playwright browser runtime install into CI/bootstrap so browser test does not skip by default.
- Final UI polish/QA pass for family-ready finish.
